# Batch 1a（DNS 1.14 化）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 generate_client.uc 与 UI 中落地 sing-box 1.14 DNS 能力：`evaluate`/`match_response`/`race`、乐观缓存与 `store_dns`、统一 `timeout`，并删除 1.14 弃用结构。

**Architecture:** UCI 增加显式选项（默认关/空）；预设分流模式提供默认关闭的 `cn_ip_fallback`；custom 模式 DNS 规则输出新字段并对旧“地址过滤”规则做生成期自动 evaluate 包装；migrate_config 幂等清理弃用字段；fixture 真机验证。

**Tech Stack:** ucode、LuCI JS (client.js)、uci-defaults。

**Spec:** `docs/superpowers/specs/2026-09-02-homeproxy1.14-singbox-1.14-features-design.md`（第 6.1–6.4 节）

## Global Constraints

- 只面向 sing-box ≥ 1.14；不再输出 `independent_cache`、DNS 规则 `strategy` 动作选项、`rule_set_ip_cidr_accept_empty`。
- 新增 UCI 选项默认值不得改变现有生成结果（`cn_ip_fallback` 默认 '0'；缓存/超时默认空/关）。
- 生成结果必须通过 `sing-box check`；先写 fixture（红）再实现（绿）再提交。
- 依赖 Batch 0 的原子落盘与 `tests/router/run_fixture.sh`。
- 除注明外，所有代码位于 `/Users/wjp/Downloads/homeproxy1.14`。

---

### Task B1a-1: UCI 默认配置与 migrate_config 清理

**Files:**
- Modify: `root/etc/config/homeproxy`
- Modify: `root/etc/homeproxy/scripts/migrate_config.uc`

**Interfaces:**
- Produces: 新 UCI 键 `config.cn_ip_fallback`、`dns.optimistic_cache`、`dns.optimistic_timeout`、`dns.dns_timeout`、`dns.cache_file_store_dns`
- Produces: migrate 后不再存在 `dns.independent_cache`、`dns.cache_file_store_rdrc`、`dns.cache_file_rdrc_timeout`、`dns_rule.domain_strategy`、`dns_rule.rule_set_ip_cidr_accept_empty`

- [ ] **Step 1: 在默认配置的 `config` 段加选项**

`root/etc/config/homeproxy` 的 `config homeproxy 'config'` 段（`option log_level 'warn'` 之后）追加：

```
	option cn_ip_fallback '0'
```

- [ ] **Step 2: 在默认配置的 `dns` 段加选项**

```
config homeproxy 'dns'
	option dns_strategy 'prefer_ipv4'
	option default_server 'local-dns'
	option disable_cache '0'
	option disable_cache_expire '0'
	option optimistic_cache '0'
	option optimistic_timeout ''
	option dns_timeout ''
	option cache_file_store_dns '0'
```

- [ ] **Step 3: migrate_config.uc 增加清理逻辑**

在 `/* routing rules options */` 段之前插入（`uciconfig`/`ucidns`/`ucidnsrule` 常量已存在）：

```js
/* sing-box 1.14: remove deprecated DNS semantics */
if (!isEmpty(uci.get(uciconfig, ucidns, 'independent_cache')))
	uci.delete(uciconfig, ucidns, 'independent_cache');

const legacy_store_rdrc = uci.get(uciconfig, ucidns, 'cache_file_store_rdrc');
if (!isEmpty(legacy_store_rdrc)) {
	uci.set(uciconfig, ucidns, 'cache_file_store_dns', legacy_store_rdrc);
	uci.delete(uciconfig, ucidns, 'cache_file_store_rdrc');
}
if (!isEmpty(uci.get(uciconfig, ucidns, 'cache_file_rdrc_timeout')))
	uci.delete(uciconfig, ucidns, 'cache_file_rdrc_timeout');

uci.foreach(uciconfig, ucidnsrule, (cfg) => {
	if (!isEmpty(cfg.domain_strategy))
		uci.delete(uciconfig, cfg['.name'], 'domain_strategy');
	if (!isEmpty(cfg.rule_set_ip_cidr_accept_empty))
		uci.delete(uciconfig, cfg['.name'], 'rule_set_ip_cidr_accept_empty');
});
```

- [ ] **Step 4: 本地检查并提交**

```bash
ucode -s root/etc/homeproxy/scripts/migrate_config.uc 2>/dev/null || true
git add root/etc/config/homeproxy root/etc/homeproxy/scripts/migrate_config.uc
git commit -m "feat(dns): add 1.14 option defaults and migrate away deprecated DNS fields"
```

---

### Task B1a-2: 预设模式 `cn_ip_fallback`（evaluate/match_response 恢复大陆 IP 兜底）

**Files:**
- Modify: `root/etc/homeproxy/scripts/generate_client.uc`（`routing_mode === 'bypass_mainland_china'` 分支内，geosite-cn 规则之后）

**Interfaces:**
- Consumes: `uci.get(uciconfig, ucimain, 'cn_ip_fallback')`
- Produces: 开启时在 `config.dns.rules` 追加一条 `evaluate`（tag `cn-fallback`）与一条 `match_response: "cn-fallback"` + `geoip-cn` 规则；关闭时不产生任何输出差异

- [ ] **Step 1: 先写红 fixture**

Create `tests/router/fixtures/client-preset-fallback/config/homeproxy`（基于 B0-5 的 `client-bypass-default`，另加）：

```
config homeproxy 'config'
	option main_node 'nodetest'
	option main_udp_node 'same'
	option dns_server '8.8.8.8'
	option china_dns_server '223.5.5.5'
	option routing_mode 'bypass_mainland_china'
	option routing_port 'common'
	option proxy_mode 'redirect_tproxy'
	option ipv6_support '0'
	option log_level 'warn'
	option cn_ip_fallback '1'

config node 'nodetest'
	option label 'test-direct'
	option enabled '1'
	option type 'direct'
```

Create `tests/router/fixtures/client-preset-fallback/expect.txt`：

```
"action": "evaluate"
"cn-fallback"
"match_response": "cn-fallback"
```

- [ ] **Step 2: 运行确认失败（尚无实现）**

Run: `sh tests/router/run_fixture.sh client-preset-fallback client`（在已部署本批代码的路由器上）
Expected: FAIL(missing expected string: `"action": "evaluate"`)

- [ ] **Step 3: 实现**

找到：

```js
		push(config.dns.rules, {
			rule_set: 'geosite-cn',
			action: 'route',
			server: 'china-dns'
		});
```

在其后追加：

```js
		/* sing-box 1.14: restore CN-IP fallback via evaluate/match_response (opt-in) */
		if (uci.get(uciconfig, ucimain, 'cn_ip_fallback') === '1') {
			push(config.dns.rules, {
				action: 'evaluate',
				server: 'main-dns',
				tag: 'cn-fallback'
			});
			push(config.dns.rules, {
				match_response: 'cn-fallback',
				rule_set: 'geoip-cn',
				action: 'route',
				server: 'china-dns'
			});
		}
```

同时把该分支上方原有的“geoip fallback not emitted”注释删除，避免误导。

- [ ] **Step 4: 运行确认通过**

Run: `sh tests/router/run_fixture.sh client-preset-fallback client`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add root/etc/homeproxy/scripts/generate_client.uc tests/router
git commit -m "feat(dns): opt-in CN-IP fallback via evaluate/match_response in preset mode"
```

---

### Task B1a-3: custom 模式 DNS 规则 1.14 输出（含旧规则自动包装）

**Files:**
- Modify: `root/etc/homeproxy/scripts/generate_client.uc`（`} else if (!isEmpty(default_outbound)) {` 分支内的 DNS rules 段）

**Interfaces:**
- Consumes: `dns_rule` 段 UCI：原字段 + 新字段 `match_response`、`evaluate_tag`、`race`、`speculative`、`disable_optimistic_cache`、`remove_client_subnet`、`dns_timeout`、`response_rcode`、`response_answer`、`response_ns`、`response_extra`、`query_client_subnet`、`query_dnssec`
- Produces: `config.dns.rules` 数组；对含 legacy 地址过滤字段且无 `match_response` 的 route 规则，自动前置 `evaluate`（内部 tag `_hp_eval_<section>`）

- [ ] **Step 1: 先写红 fixture**

Create `tests/router/fixtures/client-custom-dns/config/homeproxy`：

```
config homeproxy 'config'
	option routing_mode 'custom'
	option proxy_mode 'redirect_tproxy'
	option ipv6_support '0'
	option log_level 'warn'

config homeproxy 'routing'
	option default_outbound 'direct-out'
	option default_outbound_dns 'default-dns'

config homeproxy 'dns'
	option default_server 'default-dns'

config dns_server 'legacy_server'
	option label 'legacy-server'
	option enabled '1'
	option type 'udp'
	option server '1.1.1.1'

config dns_rule 'legacy_ip_rule'
	option label 'legacy-ip-rule'
	option enabled '1'
	option action 'route'
	option server 'legacy_server'
	list ip_cidr '1.0.0.0/8'
	option ip_is_private '1'

config dns_rule 'modern_rule'
	option label 'modern-rule'
	option enabled '1'
	option action 'evaluate'
	option server 'legacy_server'
	option evaluate_tag 'first'

config dns_rule 'modern_response'
	option label 'modern-response'
	option enabled '1'
	option action 'route'
	option server 'legacy_server'
	option match_response 'first'
	option response_rcode 'NOERROR'
	list response_answer 'example.com. 300 IN A 1.2.3.4'
	option race '1'
```

Create `tests/router/fixtures/client-custom-dns/expect.txt`：

```
"action": "evaluate"
"_hp_eval_legacy_ip_rule"
"match_response": "_hp_eval_legacy_ip_rule"
"evaluate_tag": "first"
"match_response": "first"
"response_rcode": "NOERROR"
"race": true
```

- [ ] **Step 2: 运行确认失败**

Run: `sh tests/router/run_fixture.sh client-custom-dns client`
Expected: FAIL（缺少 evaluate 包装与 response 字段）

- [ ] **Step 3: 替换 DNS rules 生成段**

把从 `/* DNS rules */` 注释到 `if (isEmpty(config.dns.rules)) config.dns.rules = null;` 之间的整段替换为：

```js
	/* DNS rules */
	/* sing-box >= 1.14: legacy address-filter rules are auto-wrapped with an
	   evaluate action; deprecated strategy/accept_empty fields are dropped. */
	const builtin_dns_rules = [];
	uci.foreach(uciconfig, ucidnsrule, (cfg) => {
		if (cfg.enabled !== '1')
			return;

		const rule = {
			ip_version: strToInt(cfg.ip_version),
			query_type: parse_dnsquery(cfg.query_type),
			network: cfg.network,
			protocol: cfg.protocol,
			domain: cfg.domain,
			domain_suffix: cfg.domain_suffix,
			domain_keyword: cfg.domain_keyword,
			domain_regex: cfg.domain_regex,
			port: parse_port(cfg.port),
			port_range: cfg.port_range,
			source_ip_cidr: cfg.source_ip_cidr,
			source_ip_is_private: strToBool(cfg.source_ip_is_private),
			source_port: parse_port(cfg.source_port),
			source_port_range: cfg.source_port_range,
			process_name: cfg.process_name,
			process_path: cfg.process_path,
			process_path_regex: cfg.process_path_regex,
			user: cfg.user,
			rule_set: get_ruleset(cfg.rule_set),
			rule_set_ip_cidr_match_source: strToBool(cfg.rule_set_ip_cidr_match_source),
			invert: strToBool(cfg.invert),
			race: strToBool(cfg.race),
			speculative: strToBool(cfg.speculative),
			action: cfg.action,
			server: get_resolver(cfg.server),
			disable_cache: strToBool(cfg.dns_disable_cache),
			disable_optimistic_cache: strToBool(cfg.disable_optimistic_cache),
			rewrite_ttl: strToInt(cfg.rewrite_ttl),
			timeout: strToTime(cfg.dns_timeout),
			client_subnet: cfg.client_subnet,
			remove_client_subnet: strToBool(cfg.remove_client_subnet),
			method: cfg.reject_method,
			no_drop: strToBool(cfg.reject_no_drop),
			rcode: cfg.predefined_rcode,
			answer: cfg.predefined_answer,
			ns: cfg.predefined_ns,
			extra: cfg.predefined_extra
		};

		if (cfg.action === 'evaluate')
			rule.tag = cfg.evaluate_tag || null;

		if (cfg.match_response && cfg.match_response !== '0')
			rule.match_response = (cfg.match_response === '1') ? true : cfg.match_response;

		rule.query_client_subnet = cfg.query_client_subnet;
		rule.query_dnssec = strToBool(cfg.query_dnssec);
		rule.response_rcode = cfg.response_rcode;
		rule.response_answer = cfg.response_answer;
		rule.response_ns = cfg.response_ns;
		rule.response_extra = cfg.response_extra;

		const legacy_filter = !isEmpty(cfg.ip_cidr) || strToBool(cfg.ip_is_private) === true;
		if (legacy_filter && !rule.match_response && cfg.action === 'route') {
			const eval_tag = '_hp_eval_' + cfg['.name'];
			push(builtin_dns_rules, {
				action: 'evaluate',
				server: get_resolver(cfg.server),
				tag: eval_tag
			});
			rule.match_response = eval_tag;
		}

		/* ip_cidr / ip_is_private are only valid with match_response in 1.14 */
		if (rule.match_response) {
			rule.ip_cidr = cfg.ip_cidr;
			rule.ip_is_private = strToBool(cfg.ip_is_private);
		}

		push(builtin_dns_rules, rule);
	});
	config.dns.rules = builtin_dns_rules;
```

注意：原段中 `strategy: cfg.domain_strategy`、`rule_set_ip_cidr_accept_empty` 不再输出；`config.dns.rules` 不再可能为 null（空数组合法），若需严格等价的空配置可保留数组。

- [ ] **Step 4: 运行确认通过**

Run: `sh tests/router/run_fixture.sh client-custom-dns client`
Expected: PASS 且 `sing-box check` 通过

- [ ] **Step 5: 提交**

```bash
git add root/etc/homeproxy/scripts/generate_client.uc tests/router
git commit -m "feat(dns): emit 1.14 response-match fields and auto-wrap legacy rules"
```

---

### Task B1a-4: 乐观缓存、统一超时与 store_dns

**Files:**
- Modify: `root/etc/homeproxy/scripts/generate_client.uc`（DNS 默认对象、experimental cache_file 段）

**Interfaces:**
- Consumes: `dns.optimistic_cache`、`dns.optimistic_timeout`、`dns.dns_timeout`、`dns.cache_file_store_dns`
- Produces: `dns.optimistic`（bool 或含 timeout 对象）、`dns.timeout`、`cache_file.store_dns`

- [ ] **Step 1: 先写红 fixture**

Create `tests/router/fixtures/client-dns-cache/config/homeproxy`（复用 `client-custom-dns` 的骨架，DNS 段改为）：

```
config homeproxy 'dns'
	option default_server 'default-dns'
	option optimistic_cache '1'
	option optimistic_timeout '1h'
	option dns_timeout '5'
	option cache_file_store_dns '1'
```

Create `tests/router/fixtures/client-dns-cache/expect.txt`：

```
"optimistic": {
"timeout": "1h"
"timeout": "5s"
"store_dns": true
```

- [ ] **Step 2: 运行确认失败**

Expected: FAIL（缺少 optimistic/store_dns）

- [ ] **Step 3: 读取新选项（两分支共用）**

在 `const routing_mode = ...` 之后追加：

```js
const dns_optimistic_cache = uci.get(uciconfig, ucidnssetting, 'optimistic_cache') || '0',
      dns_optimistic_timeout = uci.get(uciconfig, ucidnssetting, 'optimistic_timeout'),
      dns_query_timeout = uci.get(uciconfig, ucidnssetting, 'dns_timeout'),
      dns_store_dns = uci.get(uciconfig, ucidnssetting, 'cache_file_store_dns') || '0';
```

（`ucidnssetting` 已在文件顶部定义为 `'dns'`。）

- [ ] **Step 4: 修改 DNS 默认对象**

找到：

```js
config.dns = {
	servers: [
	...
	],
	rules: [],
	strategy: dns_default_strategy,
	disable_cache: strToBool(dns_disable_cache),
	disable_expire: strToBool(dns_disable_cache_expire),
	independent_cache: strToBool(dns_independent_cache),
	client_subnet: dns_client_subnet
};
```

改为（其余字段原样保留）：

```js
config.dns = {
	servers: [
		{
			tag: 'default-dns',
			type: 'udp',
			server: wan_dns,
			detour: self_mark ? 'direct-out' : null
		},
		{
			tag: 'system-dns',
			type: 'local',
			detour: self_mark ? 'direct-out' : null
		}
	],
	rules: [],
	strategy: dns_default_strategy,
	disable_cache: strToBool(dns_disable_cache),
	disable_expire: strToBool(dns_disable_cache_expire),
	client_subnet: dns_client_subnet,
	optimistic: (dns_optimistic_cache === '1') ? {
		enabled: true,
		timeout: !isEmpty(dns_optimistic_timeout) ? dns_optimistic_timeout : '3d'
	} : null,
	timeout: !isEmpty(dns_query_timeout) ? strToTime(dns_query_timeout) : null
};
```

- [ ] **Step 5: 修改 cache_file 段**

找到：

```js
	config.experimental = {
		cache_file: {
			enabled: true,
			path: '/etc/homeproxy/cache.db',
			store_rdrc: strToBool(cache_file_store_rdrc),
			rdrc_timeout: strToTime(cache_file_rdrc_timeout),
		}
	};
```

改为：

```js
	config.experimental = {
		cache_file: {
			enabled: true,
			path: '/etc/homeproxy/cache.db',
			store_dns: strToBool(dns_store_dns)
		}
	};
```

同时删除 custom 分支里对 `cache_file_store_rdrc` / `cache_file_rdrc_timeout` 的旧读取变量（`cache_file_store_rdrc = ...`、`cache_file_rdrc_timeout = ...`）。

- [ ] **Step 6: 运行确认通过**

Run: `sh tests/router/run_fixture.sh client-dns-cache client`
Expected: PASS 且 `sing-box check` 通过（`disable_cache`/`disable_expire` 均为 0 时乐观缓存合法）

- [ ] **Step 7: 提交**

```bash
git add root/etc/homeproxy/scripts/generate_client.uc tests/router
git commit -m "feat(dns): optimistic cache, unified timeout and store_dns options"
```

---

### Task B1a-5: UI（client.js）

**Files:**
- Modify: `htdocs/luci-static/resources/view/homeproxy/client.js`

**Interfaces:**
- Consumes: Task B1a-1 的 UCI 键、Task B1a-3 的 dns_rule 新字段
- Produces: 可保存上述 UCI 字段的表单控件；删除 `independent_cache`/`cache_file_store_rdrc`/每规则 `domain_strategy` 控件

- [ ] **Step 1: Routing 页加 cn_ip_fallback**

在 Routing 页 `proxy_mode` 选项之后追加：

```js
		o = s.taboption('routing', form.Flag, 'cn_ip_fallback', _('CN-IP DNS fallback (sing-box 1.14)'),
			_('When the main DNS returns a mainland China IP, re-resolve via China DNS using evaluate/match_response.'));
		o.depends('routing_mode', 'bypass_mainland_china');
		o.depends({'proxy_mode': 'custom', '!reverse': true});
		o.rmempty = false;
```

- [ ] **Step 2: DNS Settings tab 替换缓存控件**

找到 `cache_file_store_rdrc` 与 `cache_file_rdrc_timeout` 两个 option 定义并删除，替换为：

```js
		so = ss.taboption('dns', form.Flag, 'optimistic_cache', _('Optimistic DNS cache'),
			_('Return expired cache immediately and refresh in background (sing-box 1.14).'));
		so.depends('disable_cache', '0');
		so.depends('disable_cache_expire', '0');
		so.rmempty = false;

		so = ss.taboption('dns', form.Value, 'optimistic_timeout', _('Optimistic cache timeout'),
			_('Max time an expired entry may be served. Examples: 3d, 1h.'));
		so.depends('optimistic_cache', '1');

		so = ss.taboption('dns', form.Value, 'dns_timeout', _('DNS query timeout'),
			_('Default timeout per DNS query in seconds (sing-box default: 10).'));
		so.datatype = 'uinteger';

		so = ss.taboption('dns', form.Flag, 'cache_file_store_dns', _('Store DNS cache'),
			_('Persist DNS cache across restarts (sing-box 1.14, replaces Store RDRC).'));
		so.depends('disable_cache', '0');
		so.rmempty = false;
```

同段删除 `independent_cache` option 定义（保留 UI 注释说明由 transport 分键取代）。

- [ ] **Step 3: DNS Rules 字段更新**

3a. 删除 action=route 下的 `domain_strategy` option 定义。

3b. action 列表追加 evaluate/respond：

```js
		so.value('evaluate', _('Evaluate (1.14)'));
		so.value('respond', _('Respond (1.14)'));
```

同时给现有 `server` option 追加 `so.depends('action', 'evaluate')`，并保持 route 依赖不变（respond/predefined 不依赖 server）。

3c. 在 field_other 追加 1.14 控件（与现有控件同风格，全部 modalonly）：

```js
		so = ss.taboption('field_other', form.Value, 'match_response', _('Match response'),
			_('1 matches the latest untagged evaluate result; other values match an evaluate tag.'));
		so.depends('action', 'route');
		so.modalonly = true;

		so = ss.taboption('field_other', form.Value, 'evaluate_tag', _('Evaluate tag'),
			_('Optional tag for this evaluate result.'));
		so.depends('action', 'evaluate');
		so.modalonly = true;

		so = ss.taboption('field_other', form.Flag, 'race', _('Race'),
			_('Judge this response-dependent rule in parallel; first match wins (1.14).'));
		so.depends({'action': 'route', 'match_response': /[\s\S]/});
		so.modalonly = true;

		so = ss.taboption('field_other', form.Flag, 'speculative', _('Speculative'),
			_('Send the query before pending race rules are judged (1.14).'));
		so.depends('action', 'route');
		so.depends('action', 'evaluate');
		so.modalonly = true;

		so = ss.taboption('field_other', form.Flag, 'disable_optimistic_cache', _('Disable optimistic cache'),
			_('Disable optimistic DNS caching for this query.'));
		so.depends('action', 'route');
		so.depends('action', 'evaluate');
		so.modalonly = true;

		so = ss.taboption('field_other', form.Value, 'dns_timeout', _('Query timeout'),
			_('Override dns.timeout for this query, in seconds.'));
		so.datatype = 'uinteger';
		so.depends('action', 'route');
		so.depends('action', 'evaluate');
		so.modalonly = true;

		so = ss.taboption('field_other', form.Flag, 'remove_client_subnet', _('Remove EDNS client subnet'));
		so.depends('action', 'route');
		so.depends('action', 'evaluate');
		so.modalonly = true;

		so = ss.taboption('field_host', form.Value, 'query_client_subnet', _('Query EDNS client subnet'),
			_('Match the EDNS Client Subnet in the query.'));
		so.datatype = 'or(cidr, ipaddr)';
		so.modalonly = true;

		so = ss.taboption('field_host', form.Flag, 'query_dnssec', _('Match DNSSEC OK'),
			_('Match queries with the DNSSEC OK bit set.'));
		so.modalonly = true;

		so = ss.taboption('field_other', form.ListValue, 'response_rcode', _('Response RCode'),
			_('Match the evaluated response code (requires match_response).'));
		for (let rc of [ 'NOERROR', 'FORMERR', 'SERVFAIL', 'NXDOMAIN', 'NOTIMP', 'REFUSED' ])
			so.value(rc);
		so.depends({'action': 'route', 'match_response': /[\s\S]/});
		so.modalonly = true;

		so = ss.taboption('field_other', form.DynamicList, 'response_answer', _('Response answer'),
			_('Text DNS records to match in the evaluated answer.'));
		so.depends({'action': 'route', 'match_response': /[\s\S]/});
		so.modalonly = true;

		so = ss.taboption('field_other', form.DynamicList, 'response_ns', _('Response NS'));
		so.depends({'action': 'route', 'match_response': /[\s\S]/});
		so.modalonly = true;

		so = ss.taboption('field_other', form.DynamicList, 'response_extra', _('Response extra'));
		so.depends({'action': 'route', 'match_response': /[\s\S]/});
		so.modalonly = true;
```

3d. ip_cidr/ip_is_private 控件描述改为提示“需 match_response/evaluate（1.14 自动包装旧规则）”。

- [ ] **Step 4: 静态检查与提交**

```bash
node --check htdocs/luci-static/resources/view/homeproxy/client.js 2>/dev/null || true
git add htdocs/luci-static/resources/view/homeproxy/client.js
git commit -m "feat(ui): expose sing-box 1.14 DNS options and response matching"
```

（本地无 node 时跳过静态检查，以浏览器/真机 UI 验证为准。）

---

### Task B1a-6: 路由器集成验证（已授权操作）

- [ ] **Step 1: 备份并部署本批脚本与 UI**

```bash
ssh root@192.168.1.1 'cp -a /etc/homeproxy/scripts /root/hp-backup/scripts.pre-b1a'
cd /Users/wjp/Downloads/homeproxy1.14
scp root/etc/homeproxy/scripts/generate_client.uc root/etc/homeproxy/scripts/migrate_config.uc root@192.168.1.1:/etc/homeproxy/scripts/
```

- [ ] **Step 2: 跑全部 DNS fixture**

```bash
scp -r tests/router/fixtures/client-preset-fallback tests/router/fixtures/client-custom-dns tests/router/fixtures/client-dns-cache root@192.168.1.1:/root/hp-dev-tests/fixtures/
ssh root@192.168.1.1 '
sh /root/hp-dev-tests/run_fixture.sh client-preset-fallback client
sh /root/hp-dev-tests/run_fixture.sh client-custom-dns client
sh /root/hp-dev-tests/run_fixture.sh client-dns-cache client
sh /root/hp-dev-tests/run_fixture.sh client-bypass-default client
'
```

Expected: 四个 PASS。

- [ ] **Step 3: 默认配置金样 diff**

升级前 27.820.3 生成结果已在备份目录；部署本批脚本后以现有在线配置（routing_mode=bypass_mainland_china）重新生成，`diff` 两份 JSON，确认差异仅为本批预期（无 cn_ip_fallback 时不出现 evaluate/optimistic 新字段）。

- [ ] **Step 4: 恢复生产并提交修正**

```bash
ssh root@192.168.1.1 'rm -rf /etc/homeproxy/scripts; cp -a /root/hp-backup/scripts.pre-b1a /etc/homeproxy/scripts; /etc/init.d/homeproxy restart'
git add tests/router
git commit -m "test: fix DNS fixtures after router validation"
```

---

**Batch 1a 完成标准：** generate_client 在 1.14 下对预设/custom DNS 输出合法配置；旧规则自动包装；UI 可保存新字段；fixture 全 PASS；生产状态恢复。
